# UI Upgrade TODO (Portfolio)

- [x] Refactor `portfolio/project.html` to use shared design in `portfolio/styles.css`.
- [ ] Remove/trim the large inline `<style>` block in `project.html` (keep only minimal page-specific rules if absolutely needed).

- [x] Fix CSS bug in `project.html`: remove `line-height: 7;` from project description.

- [ ] Update markup/classes for project cards, controls bar, empty state, and back-to-top so they match shared theme.
- [ ] Keep existing JS functionality: search, tag click filter, grid/list toggle, back-to-top, theme toggle.
- [ ] Verify styling on all screen sizes (desktop/tablet/mobile).


